# parallax-scene-studio

A WYSIWYG editor and runtime for layered parallax scenes.

Use `LabsThemeEditor` for the Labs-style WYSIWYG editor. Use
`ParallaxSceneStudio` for the smaller standalone editor/runtime surface.

See the repository README for documentation and examples.
