# Third-Party Notices

Parallax Scene Studio is MIT licensed.

The project can integrate with or bundle the following dependencies:

| Package | License | Notes |
| --- | --- | --- |
| `parallax-js` | MIT | Bundled by the Labs-style editor/runtime. |
| `jquery` | MIT | Bundled by the Labs-style editor adapter. |
| `sortablejs` | MIT | Bundled by the Labs-style layer tree. |
| `html2canvas` | MIT | Bundled for thumbnail capture. |
| `sass` | MIT | Package build tooling for Labs SCSS. |
| `vite` | MIT | Demo and package build tooling. |
| `typescript` | Apache-2.0 | Type checking and declarations. |
| `fastapi`, `uvicorn`, `boto3`, `python-dotenv`, `pydantic` | See package licenses | Used only by the optional Python S3 backend example. |

The first public release should re-check dependency versions and include any additional notices for packages added during extraction.
